package com.onlab.gymapp.Training



/*enum class Training_Type {
    Kardio, Sulyzos_edzes, Nyujtas;

    @TypeConverter
    fun getByOrdinal(ordinal: Int): Training_Type? {
        var ret: Training_Type? = null
        for (cat in Training_Type.values()) {
            if (cat.ordinal == ordinal) {
                ret = cat
                break
            }
        }
        return ret
    }

    @TypeConverter
    fun toInt(category: Training_Type): Int {
        return category.ordinal
    }
}*/